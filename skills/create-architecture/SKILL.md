---
name: create-architecture
description: 'DEPRECATED — use mp:architecture. Kept for backward compatibility.'
---

# Deprecated — use `mp:architecture`

The nine-step micro-file workflow was replaced by the **architecture spine** model in **`mp:architecture`** (synced from upstream BMAD `bmad-architecture`).

**Create architecture:** invoke `mp:architecture` (or say "create the architecture" / "architecture spine").

The old step files in this directory are retained for reference only — do not follow them when `mp:architecture` is available.
