---
name: create-prd
description: 'DEPRECATED — use mp:prd (create intent). Kept for backward compatibility.'
---

# Deprecated — use `mp:prd`

This step-file workflow was replaced by the unified **`mp:prd`** skill (synced from upstream BMAD `bmad-prd`).

**Create a new PRD:** invoke `mp:prd` with create intent (or say "create a PRD").

The old step files in this directory are retained for reference only — do not follow them when `mp:prd` is available.
