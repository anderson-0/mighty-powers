---
description: Create the architecture spine (lifecycle technical architecture)
---

Invoke the `mp:architecture` skill to produce an architecture spine — the invariants that keep epics and stories consistent. Distinct from `/architecture` (Mermaid diagrams via `architecture-map`).
