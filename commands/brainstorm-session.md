---
description: Start a Lifecycle brainstorming session (creative techniques + memlog)
---

Invoke the `mp:brainstorm-session` skill to run a Lifecycle ideation session with creative techniques and memlog.

For Quick Track design-with-approval, use `/brainstorm` (`mp:brainstorming`) instead.
